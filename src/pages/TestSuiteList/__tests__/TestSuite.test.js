import { render, screen } from '@testing-library/react';
import TestSuite from '../TestSuite';

test('renders the test suite collapsed', () => {});

test('displays the test plans when clicking on it', () => {});

test('navigates to edit test suite page when clicking on edit', () => {});
