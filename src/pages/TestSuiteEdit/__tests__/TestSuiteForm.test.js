import { render, screen } from '@testing-library/react';
import TestSuiteForm from '../TestSuiteForm';

test('renders form for a given test suite', () => {});

test('allows changing values of every editable field', () => {});

test('allows to add a new test plan when clicking on add plan button', () => {});

test('allows delete a plan from the plan list', () => {});

test('allows to cancel the edit clicking on cancel button', () => {});

test('when clicking on save it prints the serialized test suite', () => {});
